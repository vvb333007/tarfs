Оптимизированные (SIMD) версии memcpy().
Их можно скопировать в src/ и в config.h раскомментировать CONFIG_TARFS_HAVE_OPTIMIZED_MEMCPY
После этого, при сборке, будет использоваться версия tarfs_os_memcpy() из ассемблерного файла.
Это экспериментальный код