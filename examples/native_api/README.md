**THIS IS NOT AN ARDUINO EXAMPLE!**

This example demonstrates the native TARFS API. The source code can be compiled on Linux and Windows (Cygwin) using the `make` utility.

The filesystem is expected to be stored in a file named `filesystem.tar`. The `tarfs` example directory contains a sample filesystem for this example.


